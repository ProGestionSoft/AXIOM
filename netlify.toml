[build]
  command = "pnpm install && pnpm run build:playground:generate"
  publish = "playground/dist"

[build.environment]
  NODE_VERSION = "20"


[build]
  functions = "playground/.output/server"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
